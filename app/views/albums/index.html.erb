<% provide(:title, "Albums") %>

<h2>Albums</h2>
<% @albums.each do |album| %>
  <p><%= link_to image_tag(album.cover.image.thumb.url, :title => album.name), album %> <%= link_to album.name, album %></p>
<% end %>
