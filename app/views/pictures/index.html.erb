<% provide(:title, "Pictures") %>

<h2>Pictures</h2>

<p>
  <em>Visit a picture page to see its activity feed, and to like it.</em>
</p>

<p>
  <span class="glyphicon glyphicon-plus"></span> <%= link_to "upload picture", new_picture_path %>
</p>

<% @pictures.each do |picture| %>
  <p><%= link_to image_tag(picture.image.thumb.url, :title => picture.title), picture %> <%= link_to picture.title, picture %></p>
<% end %>
