<h2>Sign in</h2>

<p>
  All demo accounts have the same password: <strong>popopopo</strong>
</p>
<ul>
  <li><strong>jpale@example.com</strong></li>
  <li><strong>anne@example.com</strong></li>
  <li><strong>corinne@example.com</strong></li>
  <li><strong>justine@example.com</strong></li>
</ul>

<br />

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, :required => false, :autofocus => true, :placeholder => "jpale@example.com" %>
    <%= f.input :password, :required => false, :placeholder => "popopopo" %>
    <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
